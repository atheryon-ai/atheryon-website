# OS files
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log

# Local env
.env.local
.env.*.local

# Node
node_modules/

# Next.js
.next/
out/

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# Agent/session working context
.context/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
# Note: tests/*.spec.ts-snapshots/ used to be ignored. We track snapshots so
# the visual suite has committed baselines on CI. Regenerate via:
#   npx playwright test --update-snapshots tests/visual.spec.ts

# Playwright MCP browser artifacts (snapshots, console logs)
.playwright-mcp/

# Ad-hoc QA screenshots saved to repo root
/footer-after-fix.png
/mib-insight-mobile.png
/mib-insight-mobile-hero.png

# Local Claude Code session state
.claude/

# Local-only working asset files (drafts, source renders, Word docs)
/assets/Atheryon_Logo_Animation_Generation*.mp4
/assets/*.docx
/assets/~$*
.gstack/
.superpowers/

# Design/spec/plan working docs — may contain MNDA-only detail.
# Never commit to this PUBLIC repo; the MNDA briefing is their home.
docs/superpowers/
