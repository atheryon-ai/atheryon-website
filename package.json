{
  "name": "atheryon-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "copy-lint": "node scripts/copy-lint.mjs",
    "deploy:test": "git push origin dev",
    "preview": "npm run build && npx serve out",
    "design-lint": "node scripts/design-lint.mjs",
    "verify:production-ready": "node scripts/design-lint.mjs && if [ ! -d out ]; then echo '\\nERROR: out/ not found. Run `next build` before verify:production-ready.' >&2 && exit 1; elif grep -rE --include='*.html' 'REPLACE_ME|\\{\\{[A-Za-z0-9_]+\\}\\}' out/; then echo '\\nERROR: unresolved placeholder found in built output above. Fill it in, or ensure the component hides its block via isPending(), before deploying to production.' >&2 && exit 1; else echo '\\u2713 No unresolved placeholders in built output.'; fi"
  },
  "dependencies": {
    "next": "^15.5.23",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/node": "^20.0.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "autoprefixer": "^10.4.0",
    "eslint": "^8.0.0",
    "eslint-config-next": "^14.2.0",
    "postcss": "^8.5.10",
    "tailwindcss": "^3.4.0",
    "typescript": "^5.0.0"
  },
  "overrides": {
    "postcss": "^8.5.10",
    "glob": "^10.5.0"
  }
}
